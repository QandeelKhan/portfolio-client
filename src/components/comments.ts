export interface CommentProps {
    authorName: string;
    commentDate: string;
    commentText: string;
    imageSource: string;
}

export interface ReplyProps {
    authorName: string;
    replyDate: string;
    replyText: string;
    imageSource: string;
}

import React, { useState } from "react";

const Navbar = (props: string | boolean) => {
    return(
        <>
        <div>navbar hello {props} 
        </div>
        <h1>my changes</h1>
        </>
    ) 
};

export default Navbar;
